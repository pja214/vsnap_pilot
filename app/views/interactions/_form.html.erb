<%= form_for(@interaction, url: @url) do |f| %>
  <% if @interaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interaction.errors.count, "error") %> prohibited this interaction from being saved:</h2>

      <ul>
      <% @interaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="fieldcontainer">

      <div class="field1">
        <%= f.label :task %><br>
        <%= f.text_field :task %>
      </div>
        <div class="field2">
        <%= f.label :url %><br>
        <%= f.text_field :url %>
      </div>
      <div class="field3">
        <%= f.label :complete %><br>
        <%= f.check_box :complete %>
      </div>
      <div class="field4">
        <%= f.label :response %><br>
        <%= f.check_box :response %>
      </div>
      <div class="field5">
        <%= f.label :comments %><br>
        <%= f.text_field :comments %>
      </div>

      
      <div class="actions">
        <%= f.submit %>
      </div>
  </div>
<% end %>
